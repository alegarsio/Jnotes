let data = [
    [10,5],
    [11,15],
    [12,25],
    [13,35]
]


let labels = [1 ,1 , 0 , 0]
let nowData = [[11,17],[10,7]]

let shufled = Array(data)
              . shuffle(labels)

let newData = shufled[0]
let newlabels = shufled[1]

let result = Array(nowData)
            . classify(newData,newlabels,1)
            . map(func(x){
                return x * 100
            })
            . collect()
            
 let acc = result.evaluate(newLabels)
println(acc)
println(result)